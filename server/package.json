{
  "name": "mindmapper-backend",
  "version": "1.0.0",
  "description": "Backend para generación de mapas mentales con DeepSeek",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "render-build": "npm install && npm install puppeteer --unsafe-perm=true --allow-root"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "puppeteer": "^21.0.0",
    "cheerio": "^1.0.0-rc.12",
    "dotenv": "^16.3.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}